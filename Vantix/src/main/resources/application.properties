# Project config
spring.application.name=Vantix

# Database config
spring.datasource.url=jdbc:mysql://localhost:3306/vantix_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=Asia/Ho_Chi_Minh
spring.datasource.username=root
spring.datasource.password=123456
spring.jpa.hibernate.ddl-auto=update

# JWT config
app.jwt.secret=MDEyMzQ1Njc4OWFiY2RlZjAxMjM0NTY3ODlhYmNkZWY
app.jwt.access-token-expiration=86400000

# Local demo: expose OTP in response when SMTP is not configured. Set false in production.
app.auth.expose-dev-otp=false

# SMTP mail config. Use a Google App Password for Gmail SMTP.
app.mail.enabled=${APP_MAIL_ENABLED:true}
app.mail.from=${SMTP_FROM:${SMTP_USERNAME:Vantix}}
app.mail.from-name=Vantix
spring.mail.host=${SMTP_HOST:smtp.gmail.com}
spring.mail.port=${SMTP_PORT:587}
spring.mail.username=${SMTP_USERNAME:skmygamer662k7@gmail.com}
spring.mail.password=${SMTP_PASSWORD:fkoi boau omoy zseh}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=false
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# Seed config
app.seed.enabled=true
app.seed.admin.username=admin
app.seed.admin.email=admin@vantix.local
app.seed.admin.password=Admin@123
app.seed.sample-employee-count=100

# Upload config
app.upload.task-dir=uploads/tasks
app.upload.employee-dir=uploads/employees
app.upload.contract-dir=uploads/contracts
spring.servlet.multipart.max-file-size=20MB
spring.servlet.multipart.max-request-size=50MB
